from setuptools import setup, find_packages
from os import path

setup(
    name='vmware_tools',
    author='ACCISA',
    version='0.0.1',
    packages=find_packages(),
    description='Python automation tool for managin vms',
    # install_requires=[
    #     ''
    # ],
)